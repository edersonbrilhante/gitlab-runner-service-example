crosscicd:
  script:
    - df -h
    - docker run -v /var/run/docker.sock:/var/run/docker.sock docker
  tags:
    - crosscicd

linux:
  image: docker:latest
  services:
  - docker:dind
  script:
    - df -h
    - docker run -v /var/run/docker.sock:/var/run/docker.sock docker
  tags:
    - linux

docker:
  image: docker:latest
  services:
  - docker:dind
  script:
    - df -h
    - docker run -v /var/run/docker.sock:/var/run/docker.sock docker
  tags:
    - docker